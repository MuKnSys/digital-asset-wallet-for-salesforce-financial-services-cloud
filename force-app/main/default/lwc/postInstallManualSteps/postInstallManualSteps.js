import { LightningElement } from 'lwc';

export default class PostInstallManualSteps extends LightningElement {
    activeSections = ['step1', 'step2', 'step3'];
    // No additional logic needed - sections are expanded by default in the template
}
